#include <bits/stdc++.h>
using namespace std;

void solve() {

}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int X,N; cin>>X>>N;
    set<int> road;
    pair<set<int>::iterator,set<int>::iterator> best;
    for (int i=0; i<N; i++) {
        int newPos; cin>>newPos;
        if (road.size()==0) {
            road.emplace(newPos);
            if (road.)
        }
    }  
}