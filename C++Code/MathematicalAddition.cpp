#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef vector<int> vi;
typedef pair<int,int> pi;

#define F first
#define S second
#define PB push_back
#define MP make_pair


void solve(ll u, ll v) {
	ll sum = u+v;
	double uMult = sum/(double)u;
	double vMult = sum/(double)v;
		
}

int main() {
	int t; cin>>t;
	for (int i=0; i<t; i++) {
		ll u,v; cin>>u>>v;
		solve(u,v);
	}
}