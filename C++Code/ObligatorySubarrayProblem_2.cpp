#include <bits/stdc++.h>
using namespace std;

int N,B,T;
vector<long long> V;

int main() {
	cin.sync_with_stdio(0);
	cin.tie(0);
	
	cin>>N>>B>>T;
	V=vector<long long>(N);
	for (int i=0; i<N; i++) {
		cin>>V[i];
	}
	
}