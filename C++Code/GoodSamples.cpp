#include <bits/stdc++.h>
using namespace std;

int N,M;
long long K;

int main() {
	cin.sync_with_stdio(0);
	cin.tie(0);
	
	cin>>N>>M>>K;
	vector<long long> sums;
	for (int i=1; i<=M; i++) {
		sums[i]=(i*i+i)/2;
		
	}
}