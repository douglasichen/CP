#include <bits/stdc++.h>
using namespace std;

int N,K,V[1000001],LOGS[1000001];

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    // input
    cin>>N>>K;
    for (int i=0; i<N; i++) cin>>V[i];

    // precomp
    int pw=1;
    LOGS[0]=1;
    for (int i=1; i<1000001; i++) {
        if (pw*K<)
    }
}